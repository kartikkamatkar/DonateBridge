# Django Core Settings
SECRET_KEY=django-insecure-prod-key-change-this-to-something-secure
DEBUG=True

# Database Configuration (MySQL)
DB_ENGINE=mysql
DB_NAME=donatebridge
DB_USER=root
DB_PASSWORD=root
DB_HOST=localhost
DB_PORT=3306

# SQLite override (optional, set to True to bypass MySQL)
USE_SQLITE=False
