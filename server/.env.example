# DonateBridge Backend - Environment Variables
# Copy this file to .env and fill in your values

# Django Settings
SECRET_KEY=django-insecure-ewil=+0)3*v*o!=x&4h446)6g(c*e_a2g9u$=mi7^$b@ij-_u9
DEBUG=True
ALLOWED_HOSTS=*

# Database - Set to True to use SQLite (recommended for local dev)
USE_SQLITE=True

# MySQL Settings (only used if USE_SQLITE=False)
DB_NAME=donatebridge
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:5173
